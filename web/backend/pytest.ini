[pytest]
testpaths = tests
addopts = -q
# The backend imports its own modules (main, database, routers) and the analysis
# scripts that live at the repository root, so both directories go on the path.
pythonpath = . ../..
